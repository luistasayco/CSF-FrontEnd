import { NameFileRutaPipe } from './name-file-ruta.pipe';

describe('NameFileRutaPipe', () => {
  it('create an instance', () => {
    const pipe = new NameFileRutaPipe();
    expect(pipe).toBeTruthy();
  });
});
